import 'package:flutter/material.dart';

class ViewSettings {
  static const mainColor = Color(0xFF27AE60);
  static const secondColor = Color(0xFF6B453C);

  static const unselectedColor = Color(0xFFC4C4C4);
  static const selectedColor = mainColor;
  static const pageBackgroundColor = Color(0xffe5e5e5);

  static const successColor = mainColor;
  static const failColor = Color(0xFFC0392B);

  static const defaultNavigationBarCornerColor = Colors.transparent;

  static const pageMargin = EdgeInsets.only(left: 15, right: 15, bottom: 20);
  static const pageTitleMargin = EdgeInsets.only(top: 20, bottom: 30, left: 5);
}